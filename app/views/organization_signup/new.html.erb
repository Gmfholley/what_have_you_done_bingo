<div class="row">

  <div class="form col col-xs-12 col-md-10 col-md-offset-1">
  <% if @user.id.blank? %>
    <h2 class="accent-font text-center"><%= "Join #{@organization.name}'s group!" %></h2>
    
    
    <%= render 'form' %>
        
  <% else %>
    <h2 class="accent-font"><%= "#{@user.first_name}, Join #{@organization.name}'s group!" %></h2>
    <div class="marign30"></div>
    <%= link_to 'Join!', organization_sign_up_path(@organization.token), method: "post", class: "btn accent-background" %>
    <%= link_to "Review", organization_path(@organization), class: "btn accent-background" %>
  <% end %>
    <div class="margin30"><div>
    
    <div class="site-explanation well">
      <h3 class="accent-font accent-background">By joining a group, you can...</h3>
      <ul>
        <li>See <%= @organization.name %>'s private bingo templates</li>
        <li>Play them yourself!</li>
      </ul>
      <h3 class="accent-font accent-background">In return, the group admin can...</h3>
        <li>See you've joined (first and last name)</li>
        <li>See (not edit) your bingos</li>
        <li>Nobody else has access -- unless you choose to share!</li>
      </ul>
      <h4 class="accent-font accent-background">Perfect for...</h4>
      <ul >
        <li>Corporate challenges</li>
        <li>Friends challenging each other</li>
        <li>Anyone who wants to have fun!</li>
      </ul>
    </div>
    
    
  </div>
  
</div>